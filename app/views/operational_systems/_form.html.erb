<%= form_for(@operational_system) do |f| %>
  <% if @operational_system.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@operational_system.errors.count, "error") %> prohibited this operational_system from being saved:</h2>

      <ul>
      <% @operational_system.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
